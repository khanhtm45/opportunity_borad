# DigitalOcean — FE và BE là HAI APP RIÊNG (không gộp 1 app).
#
# Specs:
#   .do/app-api.yaml       → BE  (oyster-app-eleoo)
#   .do/app-frontend.yaml  → FE  (shark-app-epimy)
#
# Public URLs:
#   FE:  https://shark-app-epimy.ondigitalocean.app
#   API: https://oyster-app-eleoo.ondigitalocean.app/api/v1
#
# Trên mỗi app chỉ giữ ĐÚNG 1 component (api hoặc frontend).
# FE BUILD_TIME: VITE_BASE_PATH=/  và VITE_API_URL=<API>/api/v1  (không đảo chỗ).
